# Copie ce fichier en `.env` (jamais commité, cf. .gitignore et .claude/settings.json).
# myBudget n'a pas de backend : ce fichier ne contient que la config locale/build,
# pas de secrets serveur.

# Exemple : identifiants EAS pour les builds/déploiements manuels (ne pas commiter la vraie valeur)
# EXPO_TOKEN=
